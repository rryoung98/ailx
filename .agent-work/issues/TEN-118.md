# TEN-118 — The stale-build guard in the exam start gate is dead code
status: Backlog
url: https://linear.app/tenken/issue/TEN-118/the-stale-build-guard-in-the-exam-start-gate-is-dead-code

`apps/web/app/exam/page.tsx:573-584`. `setStaleBuild(<message>)` is reachable from nowhere, for two independent reasons:

1. `startServerAttempt` catches every error and returns `null` (<issue id="511ade6c-d78b-4fb7-b0ad-9dee3c06c6f4" href="https://linear.app/tenken/issue/TEN-114/hosted-run-silently-falls-back-to-the-published-practice-deck-when-the">TEN-114</issue>), so the `catch` is unreachable.
2. Even if it rethrew, `createServerAttempt` cannot throw `DeckMismatchError` - the only `throw new DeckMismatchError` is `persistence.ts:408`, inside `fetchPresentedDeck`, which `createServerAttempt` does not call.

So `<PersistWarning warning={staleBuild}>` at `:531` is permanently null. A tab left open across a deploy - the exact scenario the code names - gets no protection, and with <issue id="511ade6c-d78b-4fb7-b0ad-9dee3c06c6f4" href="https://linear.app/tenken/issue/TEN-114/hosted-run-silently-falls-back-to-the-published-practice-deck-when-the">TEN-114</issue> gets the opposite of the intent: the run starts on the wrong instrument.

Do: fix <issue id="511ade6c-d78b-4fb7-b0ad-9dee3c06c6f4" href="https://linear.app/tenken/issue/TEN-114/hosted-run-silently-falls-back-to-the-published-practice-deck-when-the">TEN-114</issue> first, then delete this branch (the hosted-deck effect at `:250-260` already catches `DeckMismatchError` and renders `deckError`). Add a test asserting the start gate renders a failure when `createServerAttempt` rejects - there is none.

---

Found in the frontend code review of 2026-09-04. Full finding, evidence and reasoning: `/Users/rickyyoung/GitHub/ailx-frontend-review-2026-09-04.md` (A-2). Review branch `w/fe-review`.
