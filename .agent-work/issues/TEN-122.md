# TEN-122 — Hosted T3 transcript turns are fire-and-forget; one failed POST silently drops a scored stance
status: Backlog
url: https://linear.app/tenken/issue/TEN-122/hosted-t3-transcript-turns-are-fire-and-forget-one-failed-post

`apps/web/lib/instrument/hostedDeck.ts:256-275`. `record()` ends in `.catch(console.warn)` with no retry and no visible state, while the code's own comments say those rows are what the SERVER's T3 score reads for stances.

The response mirror (`apps/web/lib/data/persistence.ts:186-232`) retries from `syncedThrough`. This path does not. A single transient failure permanently removes a scored stance from the evidence the exam service will score, and the candidate is never told.

Do: put transcript turns through the same retry/`syncedThrough` mirror as responses, or at minimum surface a visible unsynced state and block finalize while turns are outstanding.

---

Found in the frontend code review of 2026-09-04. Full finding, evidence and reasoning: `/Users/rickyyoung/GitHub/ailx-frontend-review-2026-09-04.md` (B-3). Review branch `w/fe-review`.
