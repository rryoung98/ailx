// This suite reads workspace package SOURCE, not the last build. See
// `vitest.shared.ts` at the repo root for why, and
// `packages/core/test/workspaceWiring.test.ts` for the guard.
export { default } from "../../vitest.shared";
