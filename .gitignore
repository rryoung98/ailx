node_modules/
dist/
.next/
.env
.env.local
*.tsbuildinfo
coverage/
.DS_Store
apps/web/out/
# Playwright (e2e) artefacts
test-results/
playwright-report/
next-env.d.ts
.vercel
# Raw model generations staged for the practice corpus (the shipped
# re-encode is committed; the megabyte originals are not).
.ailx-generated/
__pycache__/

# Agent orchestration scratch: batch criteria, gate logs, issue dumps, plans.
# Local only. It leaked into the public tree once (PR #68) and must not again.
.agent-work/

# Gate logs from scripts/gates.sh — output, not source.
gate-*.log
